server {
    listen 80;

    location / {
        proxy_pass http://chat-front:8000;
        proxy_set_header Host $host;
    }

    location /api {
        proxy_pass http://chat-api:8080/api;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}